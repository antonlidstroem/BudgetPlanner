<Window x:Class="BudgetPlanner.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BudgetPlanner.WPF"
       xmlns:views="clr-namespace:BudgetPlanner.WPF.Views"
        xmlns:summaries="clr-namespace:BudgetPlanner.WPF.Views.Summaries"
        mc:Ignorable="d"
        Title="Budgetplanerare"
        Language="sv-SE"
        MinHeight="600" 
        MinWidth="750">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <views:TransactionListView Grid.Column="0" Margin="0,0,10,0"/>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <views:TransactionForm Grid.Row="0"/>

            <StackPanel Grid.Row="1">
                <summaries:MonthSummary DataContext="{Binding MonthSummary}"/>
                <summaries:MonthlyForecast DataContext="{Binding MonthlyForecast}"/>
                <summaries:YearSummary DataContext="{Binding YearSummary}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
