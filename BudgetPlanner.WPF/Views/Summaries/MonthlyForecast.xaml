<UserControl x:Class="BudgetPlanner.WPF.Views.Summaries.MonthlyForecast"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BudgetPlanner.WPF.Views.Summaries"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Padding="10" Margin="10" CornerRadius="6" Background="#EEF6FF">
            <StackPanel>
                <TextBlock Text="Månadsprognos" FontWeight="Bold"/>

                <TextBlock Text="Årsinkomst"/>
                <TextBox Text="{Binding AnnualIncome, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Årsarbetstid (timmar)"/>
                <TextBox Text="{Binding AnnualWorkHours, UpdateSourceTrigger=PropertyChanged}"/>

                <Separator Margin="5"/>

                <TextBlock Text="Timlön:" FontWeight="Bold"/>
                <TextBlock Text="{Binding HourlyRate, StringFormat=C}"/>

                <TextBlock Text="Månadsinkomst:" FontWeight="Bold"/>
                <TextBlock Text="{Binding MonthlyIncome, StringFormat=C}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
