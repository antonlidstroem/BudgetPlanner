<UserControl x:Class="BudgetPlanner.WPF.Views.Summaries.Overview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <ScrollViewer>
        <StackPanel Margin="10" Background="#FFF">

            <!-- Månadsöversikt -->
            <Border Padding="10" Margin="0,0,0,10" CornerRadius="6" Background="#F9F9F9">
                <StackPanel>
                    <TextBlock Text="Månadsöversikt" FontWeight="Bold" FontSize="16" Margin="0,0,0,5"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <TextBlock Text="Inkomster:" Width="150" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding MonthSummary.TotalIncome, StringFormat=C}" Foreground="Green"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <TextBlock Text="Utgifter:" Width="150" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding MonthSummary.TotalExpense, StringFormat=C}" Foreground="Red"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <TextBlock Text="Resultat:" Width="150" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding MonthSummary.Result, StringFormat=C}" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Årssummering -->
            <Border Padding="10" Margin="0,0,0,10" CornerRadius="6" Background="#FFF4E5">
                <StackPanel>
                    <TextBlock Text="Årssummering" FontWeight="Bold" FontSize="16" Margin="0,0,0,5"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <TextBlock Text="Totala inkomster:" Width="150" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding YearSummary.TotalIncome, StringFormat=C}" Foreground="Green"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <TextBlock Text="Totala utgifter:" Width="150" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding YearSummary.TotalExpense, StringFormat=C}" Foreground="Red"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <TextBlock Text="Resultat:" Width="150" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding YearSummary.Result, StringFormat=C}" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Månadsprognos -->
            <Border Padding="10" CornerRadius="6" Background="#EEF6FF">
                <StackPanel>
                    <TextBlock Text="Månadsprognos" FontWeight="Bold" FontSize="16" Margin="0,0,0,5"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <TextBlock Text="Årsinkomst:" Width="150"/>
                        <TextBox Text="{Binding MonthlyForecast.AnnualIncome, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <TextBlock Text="Årsarbetstid (timmar):" Width="150"/>
                        <TextBox Text="{Binding MonthlyForecast.AnnualWorkHours, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <Separator Margin="5"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <TextBlock Text="Timlön:" Width="150" FontWeight="Bold"/>
                        <TextBlock Text="{Binding MonthlyForecast.HourlyRate, StringFormat=C}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <TextBlock Text="Månadsinkomst:" Width="150" FontWeight="Bold"/>
                        <TextBlock Text="{Binding MonthlyForecast.MonthlyIncome, StringFormat=C}"/>
                    </StackPanel>

                    <Separator Margin="5"/>

                    <TextBlock Text="Månadsprognos inklusive återkommande poster" FontWeight="Bold" Margin="0,5,0,2"/>
                    <TextBlock Text="{Binding MonthlyForecast.MonthlyForecast, StringFormat=C}" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
