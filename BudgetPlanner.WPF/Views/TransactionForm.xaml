<UserControl x:Class="BudgetPlanner.WPF.Views.TransactionForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:BudgetPlanner.WPF.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel Margin="10">
        <!-- Rubrik -->
        <TextBlock FontWeight="Bold"
                   FontSize="14"
                   Margin="0,0,0,10"
                   Text="{Binding FormTitle}" />

        <!-- Datum -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="Datum:" Width="80"/>
            <DatePicker SelectedDate="{Binding Form.TransactionDate, Mode=TwoWay}" Width="150"/>
            <CheckBox IsChecked="{Binding Filter.FilterByDate}" />
        </StackPanel>

        <!-- Anteckning -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="Anteckning:" Width="80"/>
            <TextBox Text="{Binding Form.TransactionDescription, Mode=TwoWay}" Width="150"/>
            <CheckBox IsChecked="{Binding Filter.FilterByDescription}" />
        </StackPanel>

        <!-- Belopp -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="Belopp:" Width="80"/>
            <TextBox Text="{Binding Form.TransactionAmount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Width="150"/>
            <CheckBox IsChecked="{Binding Filter.FilterByAmount}" />
        </StackPanel>

        <!-- Kategori -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="Kategori:" Width="80"/>
            <ComboBox ItemsSource="{Binding Categories}" 
                      SelectedItem="{Binding Form.TransactionCategory, Mode=TwoWay}"
                      SelectedValuePath="Id"
                      ItemTemplate="{StaticResource CategoryComboBoxItemTemplate}"
                      ItemContainerStyle="{StaticResource CategoryComboBoxItemStyle}"
                      Width="150"/>
            <CheckBox IsChecked="{Binding Filter.FilterByCategory}" />
        </StackPanel>

        <!-- Återkommande -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Återkommande:" Width="80"/>
            <ComboBox SelectedItem="{Binding Form.TransactionRecurrence}" Width="150"
                      ItemsSource="{Binding Form.RecurrenceValues}"/>
            <CheckBox IsChecked="{Binding Filter.FilterByRecurrence}" />
        </StackPanel>

        <!-- Knappar -->
        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Lägg till" Command="{Binding AddCommand}" Style="{StaticResource EditModeButtonStyle}" Width="80" Margin="0,0,10,0"/>
            <Button Content="Uppdatera" Command="{Binding UpdateCommand}" Style="{StaticResource EditModeVisibleButtonStyle}" Width="80" Margin="0,0,10,0"/>
            <Button Content="Avbryt" Command="{Binding CancelEditCommand}" Style="{StaticResource EditModeVisibleButtonStyle}" Width="80"/>
            <Button Content="Ta bort" Command="{Binding DeleteCommand}" Width="80"
                    IsEnabled="{Binding Selected, Converter={StaticResource NullToBoolConverter}}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
