<UserControl x:Class="BudgetPlanner.WPF.Views.TransactionSummaries"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:BudgetPlanner.WPF.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="Auto"
             Width="Auto">

    <!-- Lägg till Border runt Grid -->
    <Border Background="#F9F9F9" Padding="10" CornerRadius="5" Margin="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Vänster kolumn -->
            <StackPanel Grid.Column="0" Margin="5">
                <TextBlock Text="Totala inkomster:" FontWeight="Bold"/>
                <TextBlock Text="{Binding FilteredTotalIncome, StringFormat=C}" Foreground="Green"/>
                <TextBlock Text="Totala utgifter:" FontWeight="Bold"/>
                <TextBlock Text="{Binding FilteredTotalExpense, StringFormat=C}" Foreground="Red"/>
                <TextBlock Text="Resultat:" FontWeight="Bold"/>
                <TextBlock Text="{Binding FilteredTotalResult, StringFormat=C}"/>
            </StackPanel>

            <!-- Höger kolumn -->
            <StackPanel Grid.Column="1" Margin="5">
                <TextBlock Text="Månadsprognos:" FontWeight="Bold"/>
                <TextBlock Text="{Binding FilteredMonthlyForecast, StringFormat=C}"/>
            </StackPanel>
        </Grid>
    </Border>

</UserControl>
